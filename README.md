# ft_printf

The `ft_printf` project is a part of the 42 Cursus curriculum. It aims to recreate the functionality of the standard C library function `printf`, allowing you to format and print output to the standard output or a file. This implementation supports a variety of format specifiers and modifiers, making it a versatile tool for displaying data.
